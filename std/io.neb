extern "C" {
    void __nebula_rt_print(keeps Ref<u8> buf);
    void __nebula_rt_println(keeps Ref<u8> buf);
}

void print(str s) {
    __nebula_rt_print(s.ptr);
}

void println(str s) {
    __nebula_rt_println(s.ptr);
}
