namespace std::io;

extern "C" {
    void __nebula_rt_print(keeps string buf);
    void __nebula_rt_println(keeps string buf);
}

void print(string s) {
    __nebula_rt_print(s);
}

void println(string s) {
    __nebula_rt_println(s);
}
